using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour {
	/*
	public Transform rearCameraPosition;
	public Transform defaultCameraPosition;
	public string playerController = "J1";
	private string rearButton = "RearView";
	private int currentMode = 0;

	// Use this for initialization
	void Start () {
		rearButton += playerController;
	}
	
	// Update is called once per frame
	void Update () {
		if (Input.GetButtonDown (rearButton)) {
			
		}
	
	}*/


}
